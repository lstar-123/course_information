# Course Schedule Frontend

基于纯原生 JavaScript (ES Modules) 开发的响应式课程表。

## 运行方式

由于使用了 ES Modules，必须通过 HTTP 服务器运行，不能直接打开 HTML 文件。

1. 在根目录运行: `npx serve .`
2. 打开浏览器访问: `http://localhost:3000`

## 依赖
数据源: `/dist/all_weeks_courses.json`